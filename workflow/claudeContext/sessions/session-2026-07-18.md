# Session 2026-07-18

## Summary

Decided to fully rebuild the car-database app (first project, 4 years old) rather than patch it — CRA is dead, Mongoose 6 callback API is gone in v8, both FE and BE needed a real split. Wrote `REFACTOR_PLAN.md` and executed Phase 1 (server) end to end, with Checkpoint 1 passing manual QA.

## Decided

- Vite + Express split, one repo, two independent packages (not two separate git repos — see conversation, not written up as a `decision-` file since it was a quick call).
- Sessions over JWT — [[decision-session-auth-over-jwt]]
- Structured, distinguished error codes, generic on purpose for login — [[decision-distinguished-error-codes]]
- Feature-based client folders + one-wrapper-per-dependency rule (not yet built, planned for Phase 2)
- Real car data import later via NHTSA vPIC/Kaggle, not scraping listing sites (ToS/anti-bot concerns) — noted in `REFACTOR_PLAN.md` future work
- Set up this `workflow/claudeContext/` folder itself, scaled down from a workplace system the user has and will bring more of once they're back on that machine

## Built (Phase 1 — server, complete)

- TypeScript Express server: session auth (Passport + connect-mongo), zod-validated CRUD, `isAuthenticated` guard on writes
- New `fuelTypes`/`bodyStyles` array schema replacing 15 boolean-ish fields; migration script run against the real local `autoDB` (7 cars converted, verified via curl)
- OpenAPI docs generated from the same zod schemas (not hand-written), served at `/api/docs`, grouped into Cars/Auth tags; `docs:check` diffs the generated doc against real Express routes so they can't silently drift
- Structured error system (`AppError` + central `errorHandler`), `asyncHandler` wrapper (fixed a latent bug: Express 4 doesn't auto-catch async route rejections)
- 14 tests passing (Vitest + Supertest + mongodb-memory-server), 0 `npm audit` vulnerabilities
- Docs written: `AGENTS.md` (cross-tool agent guidance), `server/LEARNING.md` (backend refresher — user is FE-strong, BE-rusty), `DEVOPS.md` (DevOps refresher — user has "some exposure"), `server/QA_CHECKLIST.md`

## Next

Phase 2 (Vite + React client) — not started. Same checkpoint discipline: build, verify, stop for manual test before Phase 2.5 (automations).
